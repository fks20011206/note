
  ---
  核心观察：

  1. Reward 区分度太弱 — 正确轨迹的 avg_reward 在 0.989~1.100 之间，差异只有 ~0.1。错误轨迹全为 0。组内 advantage 主要由"对 vs 错"决定，而不是"短
   vs 长"。
  2. Token savings 信号被淹没 — 在 mixed group 中，正确 vs 错误的 reward 差距（~1.0）远大于正确内部 NRP 前后的差距（1.1 vs 0.9），所以 advantage
   vs 长"。
  3. Token savings 信号被淹没 — 在 mixed group 中，正确 vs 错误的 reward 差距（~1.0）远大于正确内部 NRP 前后的差距（1.1 vs 0.9），所以 advantage
  几乎只编码了"答对/答错"，没有编码"简洁/冗余"。
  4. 错误轨迹全部打满 max_tokens — 都是 ~4000 tokens，说明模型在不确定时会一直生成到截断，但 reward=0 的 flat signal
  几乎只编码了"答对/答错"，没有编码"简洁/冗余"。
  5. 错误轨迹全部打满 max_tokens — 都是 ~4000 tokens，说明模型在不确定时会一直生成到截断，但 reward=0 的 flat signal
  没有告诉模型"哪里开始走错了"。

两段式训练，前几轮先训GSM8K的，再训math的，eval集使用两个数据集的各500条（都要纯数题目）。






llama factory
slim